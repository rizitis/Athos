
Homepage="https://kde.org/applications/system/"
        #|--------------------------------------|#
SortDesc="Configure and schedule tasks"
LongDesc="Configure and schedule tasks"
VERSION=25.04.1
PRGNAM=kcron
SRCNAM=kcron
SRCURL=https://download.kde.org/stable/release-service/25.04.1/src/kcron-25.04.1.tar.xz
COMPRESS=tar.xz

REQUIRED="extra-cmake-modules	kcmutils	kconfigwidgets	kdoctools	ki18n	kiconthemes	kio"
OPTDEPS=

BUILD=${BUILD:-1}


# This must be always here even if not patches needed.
patch_build() {
if [ -e $CWD/patches/ ]; then
  for PATCH in $CWD/patches/*.patch; do
    patch -p1 -i $PATCH || exit
  done
fi
}


# example for 90% of plasma builds.
slackbuild() {
cmake -B build -G Ninja \
		-DCMAKE_BUILD_TYPE=RelWithDebInfo \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_INSTALL_DOCDIR=/usr/doc \
		-DCMAKE_INSTALL_MANDIR=/usr/man \
		-DCMAKE_INSTALL_LIBEXECDIR=/usr/libexec \
		-DCMAKE_INSTALL_LIBDIR=lib64

	cmake --build build
	DESTDIR="$PKG" cmake --install build
}
